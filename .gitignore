node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.DS_Store

# Environment secrets — never commit
.env
.env.local
.env.*.local

# Phase evidence screenshots — large binary artifacts, kept local only
prototype-evidence/

# Working reference material — screen recordings and site screenshots used while
# building. Not needed by the app or the build; references/brand/ IS kept because
# the expression asset pipeline and the render briefs read the mascot source from it.
references/cosmos/
references/social/
